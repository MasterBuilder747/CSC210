package Week03;

import java.util.Scanner;

public class rounding {
//round the result
    //test if each value is pos or neg before rounding

    double round1 = 0;
    double bround1 = 0;
    double output1 = 0;

    Scanner r = new Scanner(System.in);
    double output = r.nextDouble();

    //round the result
    //test if each value is pos or neg before rounding
    /*
    if (output > 0) {
        bround1 = (output1 + 0.0005) * 1000;
        //System.out.println(bround1);
        round1 = (int) (bround1);
        double around1 = round1 / 1000.0;
        //System.out.println(around1);
    } else if (output < 0) {
        bround1 = (output1 - 0.0005) * 1000;
        //System.out.println(bround1);
        round1 = (int) (bround1);
        double around1 = round1 / 1000.0;
        //System.out.println(around1);
    } else {
        //round2 = (int) (output2 * 1000);
        //System.out.println(round2);
        //double around2 = round2 / 1000.0;
        //System.out.println("There are two roots of " + around1 + " and " + around2 + ".");
    }
    */

}
